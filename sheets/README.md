# Boxtribute Google Sheets™ Add-on

The (private) repository for the add-on code is [here](https://github.com/boxwise/boxtribute-sheets).

This directory only serves as a public endpoint for the latest add-on version.
