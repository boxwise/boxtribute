type Query {
  resolveLink(code: String!): ResolvedLinkResult

  standardProducts: [StandardProductInfo!]!
  newlyRegisteredBeneficiaryNumbers(start: Date, end: Date, duration: Int): Int
  newlyCreatedBoxNumbers(start: Date, end: Date, duration: Int): Int
}
